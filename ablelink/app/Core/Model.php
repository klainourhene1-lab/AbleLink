<?php
namespace App\Core;

/**
 * Base Model class
 * Models should only contain properties, constructors, getters and setters
 * No SQL queries or business logic should be in Models
 */
class Model {
    // Base model class - intentionally minimal
    // All SQL operations should be in Controllers
}
